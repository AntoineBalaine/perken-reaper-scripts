-- ?? list of plugins to be used for each category (eq/comp/gate/etc.)
local settings = {
    show_start_up_message = true,
    show_feedback_window = true, -- should the UI display?
    show_plugin_ui = true,       -- show plugin UI when tweaking corresponding knob.
}

return settings
